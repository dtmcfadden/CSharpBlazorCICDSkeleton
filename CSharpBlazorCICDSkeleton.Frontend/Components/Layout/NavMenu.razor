<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">CSharpBlazorCICDSkeleton.Frontend</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>

        @if (NavMenuLinksService is not null)
        {
            @foreach (var link in NavMenuLinks)
            {
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="@link.HrefValue">
                        <span class="bi @link.ClassName" aria-hidden="true"></span> @link.DisplayName
                    </NavLink>
                </div>
            }
        }
    </nav>
</div>